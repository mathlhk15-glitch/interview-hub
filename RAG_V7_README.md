# Interview Hub v7.4 RAG-lite 설계

## 구조
학생부는 Full Context 분석을 유지하고, 외부 면접 지식만 RAG-lite 검색합니다.

`학생부 전체 분석 -> 핵심 활동/개념 -> 교육청 면접자료 검색 -> 질문 생성 참고 -> evidenceQuote 원문 재대조 -> 실전훈련`

## 데이터
- 원본: 사용자 제공 `교육청면접자료_압축본.zip`
- 추출 후보: 4,894건
- 각 행: 출처, 페이지, 자료유형, 질문, 카테고리, 키워드, 짧은 주변문맥, 품질등급
- 품질등급: `verified / usable / suspicious / reject`
- 검색 대상: verified + usable만 사용

### 제외 자료
- doc06: `2026 제시문 기반면접 대비 자료집(인문사회)` - PDF 텍스트 인코딩/추출 품질 문제
- doc10: `2026 제시문기반 면접대비 자료집(수학)` - PDF 텍스트 인코딩/수식 추출 품질 문제

## 검색
키워드·질문본문·카테고리·면접유형·학과 메타데이터를 가중 합산합니다. 대학명은 주변 문맥에 우연히 등장할 수 있으므로 약한 보조 신호로만 사용합니다. 간호학과라는 이유만으로 MMI를 자동 선택하지 않습니다.

## 지연 로딩
`rag-data.js`는 첫 화면에서 불러오지 않습니다. AI 프롬프트 생성 단계에서 `ensureRagDataLoaded()`가 동적으로 로드합니다. 정적 파일 구조와 `file://` 호환성을 위해 JSON fetch 대신 script lazy-load를 사용합니다.

## 근거 검증
`evidenceQuote`는 먼저 정규화 완전일치를 검사하고, 실패하면 인용 길이와 비슷한 sliding window를 학생부/직접입력 record에서 탐색하여 3-gram Dice 유사도를 계산합니다. 직접 입력 자료는 학생부 원문으로 표시하지 않습니다.

## RAG 미리보기
프롬프트 생성 화면에서 Top 5 검색 결과의 질문, 출처, 페이지, 점수, 품질, 매칭 키워드를 볼 수 있습니다. 이 화면은 검색 품질 검수용이며 현재 지원 대학의 공식 최신 기출이라는 뜻이 아닙니다.

## 개인정보
녹음은 서버로 자동 업로드하지 않습니다. STT는 브라우저 기능이므로 브라우저/OS에 따라 외부 서비스 처리가 개입할 수 있어 기본 OFF입니다. 백업에서도 transcript는 별도 선택해야 저장됩니다.


## v7.3 RAG 정교화
- RAG 검색 근거(매칭 핵심어·선정 이유)를 화면에서 확인할 수 있습니다.
- 교사는 검색 결과에 👍/👎 피드백을 남길 수 있으며 현재 세션의 다음 검색부터 반영됩니다. 피드백은 서버로 전송되지 않습니다.
- 유사한 질문이 상위 결과를 독점하지 않도록 간단한 다양성 재정렬을 적용합니다.
- iPhone/iPad에서는 STT가 제한될 수 있어 녹음+직접입력을 우선 안내합니다.


## v7.4 UI 접근성
RAG/질문 생성 로직은 v7.3 구조를 유지합니다. v7.4에서는 저장된 예상질문 전체 인쇄 기능을 상단 고정바, 화면별 빠른 버튼, 학생 홈, AI 결과, 훈련, 준비 현황, 면접 직전 화면, 하단 흐름 메뉴에서 쉽게 찾을 수 있도록 접근성을 강화했습니다.
